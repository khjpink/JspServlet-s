<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
<!-- 마이바티스 db관리하는 설정 파일임  -->
<!-- 여기에 있는 내용을 따로 파일로 관리하기 위해서 -->
<!-- 자동완성이 되는이유는 pom.xml의 디팬던시에 쓴 라이브러리 내용중 프로퍼티스 이용할거야 쓰는거임   -->


<!-- 마이바티스로 DB연결하려면 웹앱에서 어떤 데이터가 들어가는지 확인하고
데이터에 맞는 테이블 만들어야지, 그 테이블에 연결하기 위해 config.xml에서 환경설정
어떤 드라이버를 사용해서 db연결을 할건지  db.properties에 있다, 이 db에 연결해서 커넥션 연결했다 해재할건데    
db일 시킬 수 잇는 sql문을 정리하는곳은 mapper.xml이다 -->  
<properties resource="com/smhrd/database/db.properties" />
	
  <environments default="development">
    <environment id="development">
      <transactionManager type="JDBC"/>
      <dataSource type="POOLED">
        <property name="driver" value="${driver}"/>
        <property name="url" value="${url}"/>
        <property name="username" value="${username}"/>
        <property name="password" value="${password}"/>
      </dataSource>
    </environment>
  </environments>
  <mappers>
    <mapper resource="com/smhrd/database/Mapper.xml"/>
  </mappers>
</configuration>